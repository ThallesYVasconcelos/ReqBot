fix: OOM ao salvar/indexar requisito — embedding store best-effort + menos heap

- addToEmbeddingStore e removeFromEmbeddingStore: captura OutOfMemoryError e
  Exception, loga warning e nao propaga o erro — o requisito e salvo no banco
  mesmo que o embedding store esteja sem memoria
- findRelevantContext em RequirementService: fallback para contexto sem embedding
  se o modelo ONNX estiver indisponivel
- getGeneralReport: retorna lista vazia de conflitos em vez de OOM
- getAnonymousQuestionRanking em WorkspaceService: retorna lista vazia em OOM
- Dockerfile: MaxRAMPercentage 65 -> 55 para reservar mais memoria nativa ao ONNX
